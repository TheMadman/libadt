function(testcase target)
	add_executable(test_${target} ${target}.c)
	target_link_libraries(test_${target} adt)
	add_test(NAME ${target} COMMAND test_${target})
endfunction()

testcase(libadt_lptr)
testcase(libadt_str)
testcase(libadt_vector)
testcase(libadt_bitwise_array)
